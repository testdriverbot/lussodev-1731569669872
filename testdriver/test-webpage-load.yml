version: 4.1.20
steps:
  - prompt: Focus the browser window.
    commands:
      - command: hover-text
        text: Lusso Labs
        description: browser tab title
        action: click
  - prompt: Assert that the webpage loads successfully.
    commands:
      - command: assert
        expect: ONE GLOBAL AI MARKETPLACE is visible on the webpage
  - prompt: >-
      Assert that the "LUSSO.AI" logo is visible.  # Use the content of the file
      from the environment
    commands:
      - command: assert
        expect: LUSSO.AI logo is visible on the webpage
